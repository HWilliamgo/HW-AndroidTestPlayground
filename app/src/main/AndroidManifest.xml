<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.william.kotlinsimpletest">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 连麦 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".rv_test.RvTestActivity"></activity>
        <activity android:name=".fragment_dialog.FragmentDialogActivity" />
        <activity android:name=".htmlTextView.HtmlTextViewActivity" />
        <activity android:name=".braodcast.ConnectivityTestActivity" />
        <activity
            android:name=".stack_test.StackTest2Activity"
            android:launchMode="singleInstance"
            android:taskAffinity="com.willia.qwe" />
        <activity android:name=".stack_test.StackTestActivity" />
        <activity android:name=".kotlin_corountine.CoroutinesActivity" />
        <activity android:name=".viewTest.ViewTestActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MusicService"
            android:exported="true" />

        <activity android:name=".lockScreen.FiredLockScreenActivity" />
        <activity
            android:name=".lockScreen.LockScreenActivity"
            android:launchMode="singleInstance" />

        <service android:name=".lockScreen.LockScreenService" />
    </application>

</manifest>